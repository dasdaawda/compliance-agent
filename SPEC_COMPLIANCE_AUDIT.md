# 📋 Аудит соответствия техническому заданию MVP "AI-Комплаенс Агент"

**Дата аудита:** 27 ноября 2024  
**Версия ТЗ:** Модули 1-6  
**Статус проекта:** Частично реализован с критическими пробелами

---

## 🎯 Executive Summary

Проект реализует **70-75%** функциональности, описанной в ТЗ. Архитектура соответствует спецификации (Django монолит, Celery, PostgreSQL, B2, HTMX), но есть **критические пробелы** в реализации ключевой функциональности "Модели Кентавр":

### ✅ Что реализовано хорошо:
- Архитектура системы (Django 5, Celery, PostgreSQL с JSONB, B2 + Cloudflare)
- Двухтабличная модель данных (AITrigger + OperatorLabel) согласно философии сбора данных
- Пользовательские роли (Client, Operator, Admin)
- REST API + HTMX интерфейсы
- Рабочий стол оператора (3-панельный интерфейс)
- Resilient pipeline с retry логикой
- Видеовалидация

### ❌ Критические недостатки:
1. **НЕПОЛНАЯ КАРТА РАЗМЕТКИ** - только 6 из 16+ требуемых категорий лейблов
2. **ОТСУТСТВИЕ СЛОВАРЕЙ NLP** - нет файлов PROFANITY_LIST, BRAND_LIST, STOPWORDS_LIST
3. **НЕТ ЗАГРУЗКИ ВИДЕО ПО URL** (yt-dlp) - только файловая загрузка
4. **ОТСУТСТВУЕТ ДОКУМЕНТАЦИЯ** - нет каталога `docs/` с API.md, ARCHITECTURE.md, DEVELOPMENT.md
5. **БАГИ В КОДЕ** - критическая ошибка в RiskDefinition.code default

---

## 📊 Детальный анализ по модулям

### Модуль 1: Архитектура системы ✅ 95% соответствие

**Реализовано:**
- ✅ Django 5 + Python 3.11 монолит
- ✅ Celery + Redis для async обработки
- ✅ PostgreSQL с JSONB для AI отчетов
- ✅ Backblaze B2 интеграция (`storage/b2_utils.py`)
- ✅ HTMX + Bootstrap 5 frontend
- ✅ Replicate для AI inference
- ✅ Модульные настройки (dev.py / prod.py)
- ✅ Docker Compose для разработки

**Проблемы:**
- ⚠️ Cloudflare CDN интеграция частично реализована (есть signed URLs, но нет автоматического кэширования)
- ⚠️ DigitalOcean App Platform не упоминается в документации деплоя (только Docker Compose)

**Оценка:** 9.5/10

---

### Модуль 2: Пользовательские роли ✅ 100% соответствие

**Реализовано:**
- ✅ User model с ролями (CLIENT, OPERATOR, ADMIN) в `users/models.py`
- ✅ `balance_minutes` для клиентов
- ✅ `performance_metric` для операторов
- ✅ Permission classes: `IsClient`, `IsOperator`, `IsAdmin`
- ✅ Role-based access control во всех ViewSets

**Оценка:** 10/10

---

### Модуль 3: User Stories ⚠️ 60% соответствие

#### Epic: Онбординг клиентов (US-C1 до US-C10)

| Story | Статус | Комментарий |
|-------|--------|-------------|
| US-C1 | ✅ | Регистрация через REST API + Django auth |
| US-C2 | ✅ | Project model реализован |
| US-C3 | ✅ | Загрузка файлов с валидацией (2GB, форматы) |
| US-C4 | ❌ | **НЕТ загрузки по URL** (yt-dlp в requirements.txt, но не используется) |
| US-C5 | ✅ | VideoStatus: UPLOADED → PROCESSING → VERIFICATION → COMPLETED |
| US-C6 | ✅ | Email уведомления (`send_video_ready_notification`) |
| US-C7 | ✅ | `ReportDetailPartialView` для просмотра отчета |
| US-C8 | ✅ | Кликабельные тайм-коды в HTMX интерфейсе |
| US-C9 | ✅ | Скриншоты/расшифровки в отчетах |
| US-C10 | ⚠️ | `balance_minutes` есть, но нет UI для пополнения |

**Критичный баг:**
```python
# projects/models.py - US-C4 не реализован
# Video model имеет поле video_url, но нет логики скачивания через yt-dlp
```

#### Epic: Рабочий стол оператора (US-O1 до US-O10)

| Story | Статус | Комментарий |
|-------|--------|-------------|
| US-O1 | ✅ | `TaskQueueService.get_next_task()` с FIFO |
| US-O2 | ✅ | `VerificationTask.assign_to_operator()` с блокировкой |
| US-O3 | ✅ | 3-панельный интерфейс (`operators/workspace.html`) |
| US-O4 | ✅ | Синхронизация плеера по клику на триггер (HTMX + JS) |
| US-O5 | ⚠️ | Динамические кнопки есть, но **неполная карта разметки** |
| US-O6 | ✅ | `LabelingService.create_operator_label()` |
| US-O7 | ✅ | Поддержка "OK (ложный)" лейбла |
| US-O8 | ✅ | Запись в `Operator_Labels` + логирование в `OperatorActionLog` |
| US-O9 | ❌ | **НЕТ UI для ручного добавления** (API есть, кнопка в UI отсутствует) |
| US-O10 | ✅ | `CompleteVerificationView` |

**Оценка Epic:** 7/10

#### Epic: Администрирование (US-A1 до US-A5)

| Story | Статус | Комментарий |
|-------|--------|-------------|
| US-A1 | ✅ | Django Admin для управления клиентами |
| US-A2 | ✅ | Ручное изменение `balance_minutes` |
| US-A3 | ✅ | Управление операторами через Admin |
| US-A4 | ⚠️ | Задачи создаются автоматически, но можно назначить вручную |
| US-A5 | ❌ | **НЕТ дашборда статистики** в Admin |

**Оценка Epic:** 6/10

**Общая оценка модуля 3:** 6.5/10

---

### Модуль 4: AI/ML подсистема ⚠️ 75% соответствие

#### 4.1 Celery DAG пайплайна ✅

**Реализовано:**
- ✅ `ai_pipeline/celery_tasks.py` с граф-ом задач
- ✅ `process_video` → `run_whisper_asr` + `run_video_analytics` → `compile_report`
- ✅ Retry логика с exponential backoff
- ✅ Идемпотентное выполнение с `last_step`, `retry_count`

**Проблемы:**
- ⚠️ FFmpeg задачи не выделены в отдельные Celery tasks (должны быть Task 2 и Task 3 по ТЗ)

#### 4.2 FFmpeg препроцессинг ✅

**Реализовано:**
- ✅ `VideoPreprocessor`, `AudioProcessor`, `FrameProcessor` в `services/ffmpeg_service.py`
- ✅ Команда извлечения аудио: `pcm_s16le -ar 16000 -ac 1` ✅
- ✅ Команда нарезки кадров: `-vf fps=1` ✅

#### 4.3 Аудио-пайплайн (ASR + NLP) ⚠️

**Реализовано:**
- ✅ `WhisperASRService` с вызовом Replicate API
- ✅ Модель: `openai/whisper-small` (согласно ТЗ)
- ✅ `NLPDictionaryService` для поиска по словарям

**КРИТИЧНЫЙ БАГ:**
```python
# ai_pipeline/services/ai_services.py:189-199
class NLPDictionaryService:
    def __init__(self):
        self.profanity_list = self._load_dictionary(
            settings.PROFANITY_DICT_PATH, 
            default=['мат1', 'мат2', 'мат3']  # ❌ Заглушка!
        )
        self.brand_list = self._load_dictionary(
            settings.BRAND_DICT_PATH,
            default=['coca cola', 'pepsi', 'nike', 'adidas']  # ❌ Заглушка!
        )
        # ...
```

**ПРОБЛЕМА:** Нет реальных словарей! ТЗ (Модуль 4.3) требует:
- `PROFANITY_LIST` - список мата (русский)
- `BRAND_LIST` - популярные бренды
- `STOP_WORDS_LIST` - слова для 436-ФЗ, 20.3 КоАП

**Действие:** Нужно создать файлы:
- `backend/ai_pipeline/dictionaries/profanity_ru.txt`
- `backend/ai_pipeline/dictionaries/brands.txt`
- `backend/ai_pipeline/dictionaries/stopwords_legal.txt`

#### 4.4 Видео-пайплайн ✅

**Реализовано:**
- ✅ `VideoAnalyticsService` с 4 моделями:
  - ✅ Falconsai NSFW detector
  - ✅ ViT Violence detector
  - ✅ YOLOv8 для объектов
  - ✅ EasyOCR для текста
- ✅ Вызовы через Replicate API
- ✅ Агрегация триггеров в `AITrigger` таблицу

**Оценка модуля 4:** 7.5/10

---

### Модуль 5: UI/UX "Рабочий стол Оператора" ⚠️ 80% соответствие

#### 5.1 Wireframe интерфейса ✅

**Реализовано:** `operators/workspace.html`
- ✅ **Панель 1:** Видеоплеер (левая часть, 60% ширины)
  - ✅ HTML5 `<video>` с signed URLs из B2
  - ✅ Горячие клавиши: Space (play/pause), J/L (±5 сек)
- ✅ **Панель 2:** Лента AI-триггеров (правая часть, верх, 40%)
  - ✅ Вертикальная прокрутка
  - ✅ Кликабельные карточки с тайм-кодами
  - ✅ Статус: "Непроверено" → "Проверено"
- ✅ **Панель 3:** Панель классификации + итоговый отчет (правая часть, низ, 40%)
  - ✅ Динамическая загрузка через HTMX
  - ✅ Кнопки-лейблы
  - ✅ Комментарии

**Проблемы:**
- ❌ Нет кнопки **"+ Добавить риск вручную"** (US-O9) - упоминается в коде, но не рендерится
- ⚠️ Итоговый отчет отображается отдельно, не в том же блоке (мелкое отклонение от wireframe)

#### 5.2 Карта разметки (Таблица 3 из ТЗ) ❌ КРИТИЧНЫЙ НЕДОСТАТОК

**Требования по ТЗ (Таблица 3):**

| AI-Триггер | Требуемые лейблы |
|------------|------------------|
| Object-General (YOLO) | [OK], [Реклама (Бренд)], [Алкоголь (в кадре)], [Табак/Вейп] |
| Text-General (OCR) | [OK], [Реклама (Сайт/Телефон)], [Мат (в тексте)], [Запрещ. информация] |
| Word-Profanity (Whisper) | [OK (Ложный/Оговорка)], [Мат (в речи)] |
| Word-Brand (Whisper) | [OK (Упоминание)], [Рекламная интеграция (Нативная)] |
| Image-NSFW (Falconsai) | [OK], [Эротика (16+)], [Порнография (18+)] |
| Image-Violence (ViT) | [OK], [Насилие/Жесть (18+)], [Драка/Кровь (16+)] |
| Object-Symbol (YOLO-custom) | [OK], [Запрещ. символика], [Тату (Общее)] |
| Manual-Add | (Все категории) |

**Текущая реализация в коде:**

```python
# operators/models.py:62-68
class OperatorLabel(models.Model):
    class FinalLabel(models.TextChoices):
        OK = "ok", _("OK")
        OK_FALSE = "ok_false", _("OK (ложное срабатывание)")
        AD_BRAND = "reklama_brand", _("Реклама (бренд)")
        PROFANITY_SPEECH = "mat_speech", _("Мат (речь)")
        PORNOGRAPHY_18 = "nsfw_18", _("Порнография (18+)")
        VIOLENCE_18 = "violence_18", _("Насилие (18+)")
```

**ОТСУТСТВУЮТ лейблы:**
- ❌ `ALCOHOL_VISIBLE` - "Алкоголь (в кадре)"
- ❌ `TOBACCO_VAPE` - "Табак/Вейп (в кадре)"
- ❌ `AD_WEBSITE_PHONE` - "Реклама (Сайт/Телефон)"
- ❌ `PROFANITY_TEXT` - "Мат (в тексте)"
- ❌ `PROHIBITED_INFO` - "Запрещ. информация (Призыв)"
- ❌ `AD_NATIVE` - "Рекламная интеграция (Нативная)"
- ❌ `EROTICA_16` - "Эротика (16+)"
- ❌ `FIGHT_BLOOD_16` - "Драка/Кровь (16+)"
- ❌ `PROHIBITED_SYMBOLS` - "Запрещ. символика (Свастика, etc.)"
- ❌ `TATTOO_GENERAL` - "Тату (Общее)"

**Текущий маппинг в `LabelingService`:**

```python
# operators/services.py:44-49
mapping_pairs = {
    AITrigger.TriggerSource.YOLO_OBJECT: [FinalLabel.OK, FinalLabel.AD_BRAND],
    AITrigger.TriggerSource.WHISPER_PROFANITY: [FinalLabel.OK_FALSE, FinalLabel.PROFANITY_SPEECH],
    AITrigger.TriggerSource.FALCONSAI_NSFW: [FinalLabel.OK, FinalLabel.PORNOGRAPHY_18],
    AITrigger.TriggerSource.VIOLENCE_DETECTOR: [FinalLabel.OK, FinalLabel.VIOLENCE_18],
}
```

**ПРОБЛЕМЫ:**
1. Для `YOLO_OBJECT` должно быть: [OK, Реклама, **Алкоголь, Табак**]
2. Нет маппинга для `EASYOCR_TEXT`
3. Нет маппинга для `WHISPER_BRAND`
4. NSFW должен различать 16+ и 18+
5. Violence должен различать жесть 18+ и драку 16+

**КРИТИЧНОСТЬ:** ❌❌❌ **Высокая!** Это нарушает философию "Data-as-an-Asset" (Модуль 1, п.2). Без полной карты разметки система не собирает качественный датасет для v2.0.

**Оценка модуля 5:** 6/10

---

### Модуль 6: Нефункциональные требования ✅ 85% соответствие

#### 6.1 Границы MVP ✅

Все ограничения MVP v1.0 соблюдены:
- ✅ Нет автобиллинга (ручное пополнение)
- ✅ Нет плагинов для видеоредакторов
- ✅ Нет автономного AI (все через оператора)
- ✅ Нет авто-редактирования (blur/beep)
- ✅ Нет live-стримов

#### 6.2 Производительность ⚠️

**Требование:** Обработка 10-минутного ролика за < 10 минут (RTF < 1.0)

**Реализовано:**
- ✅ Parallel execution: аудио и видео пайплайны идут параллельно
- ✅ Celery с chord/group для оркестрации
- ⚠️ Нет бенчмарков в коде (неясно, достигается ли цель)

**Рекомендация:** Добавить метрики в `PipelineExecution.processing_time_seconds`

#### 6.3 Безопасность ✅

- ✅ Приватный B2 bucket с signed URLs
- ✅ Хэширование паролей (Django AbstractUser)
- ✅ Multi-tenancy изоляция (`project__owner=request.user`)
- ✅ JWT + CSRF защита

#### 6.4 Схема БД ✅ 100% соответствие

**Таблица 4 из ТЗ полностью реализована:**

```python
# ai_pipeline/models.py
class AITrigger(models.Model):
    id = UUIDField
    video = ForeignKey(Video)
    timestamp_sec = DecimalField
    trigger_source = CharField(choices=TriggerSource.choices)
    confidence = DecimalField
    data = JSONField  # ✅ JSONB для сырых данных
    status = CharField(choices=['pending', 'processed'])
    # ...

# operators/models.py
class OperatorLabel(models.Model):
    id = UUIDField
    video = ForeignKey(Video)
    operator = ForeignKey(User)
    ai_trigger = ForeignKey(AITrigger, null=True)  # ✅ nullable для ручных
    start_time_sec = DecimalField
    end_time_sec = DecimalField(null=True)
    final_label = CharField(choices=FinalLabel.choices)  # ❌ неполный enum
    comment = TextField
    # ...
```

**Оценка модуля 6:** 8.5/10

---

## 🐛 Критические баги в коде

### Bug #1: RiskDefinition.code default ❌ КРИТИЧНЫЙ

**Файл:** `backend/ai_pipeline/models.py:103`

```python
class RiskDefinition(models.Model):
    code = models.CharField(
        _('код риска'), 
        max_length=50, 
        unique=True, 
        db_index=True, 
        default=uuid.uuid4,  # ❌ БАГ!
        help_text=_('Код из таблицы рисков')
    )
```

**Проблема:**
- `uuid.uuid4` вызывается **один раз** при определении класса
- Все экземпляры RiskDefinition получат **одинаковый UUID**
- Нарушается constraint `unique=True` → `IntegrityError`

**Решение:**
```python
# Вариант 1: Убрать default (требовать явное указание)
code = models.CharField(..., unique=True, db_index=True)

# Вариант 2: Использовать callable
def generate_risk_code():
    return f"RISK_{uuid.uuid4().hex[:8].upper()}"

code = models.CharField(..., default=generate_risk_code)
```

---

### Bug #2: Отсутствие словарей NLP ❌ БЛОКИРУЕТ ФУНКЦИОНАЛЬНОСТЬ

**Файлы не существуют:**
- `PROFANITY_DICT_PATH`
- `BRAND_DICT_PATH`
- `STOPWORDS_DICT_PATH`

**Последствия:**
- `NLPDictionaryService` использует заглушки
- Триггеры `whisper_profanity`, `whisper_brand` работают на поддельных данных
- Система не выявляет реальные риски

**Решение:**
1. Создать каталог `backend/ai_pipeline/dictionaries/`
2. Добавить файлы с реальными словарями (источники: открытые датасеты)
3. Обновить `settings/base.py`:
```python
PROFANITY_DICT_PATH = BASE_DIR / 'ai_pipeline' / 'dictionaries' / 'profanity_ru.txt'
BRAND_DICT_PATH = BASE_DIR / 'ai_pipeline' / 'dictionaries' / 'brands.txt'
STOPWORDS_DICT_PATH = BASE_DIR / 'ai_pipeline' / 'dictionaries' / 'stopwords_legal.txt'
```

---

### Bug #3: Отсутствие загрузки видео по URL ⚠️ ФУНКЦИЯ НЕ РАБОТАЕТ

**Файл:** `backend/projects/models.py:86`

```python
class Video(models.Model):
    # ...
    video_url = models.URLField(_('URL видео'), max_length=2000, null=True, blank=True)
    # ...
```

**Проблема:**
- Поле `video_url` существует
- `yt-dlp` добавлен в `requirements.txt`
- Но **нет кода скачивания** в Celery tasks

**Где должно быть:**
`ai_pipeline/celery_tasks.py` → новая задача `download_video_from_url(video_id)`

**Решение:**
```python
# ai_pipeline/services/video_downloader.py
import yt_dlp

class VideoDownloader:
    def download(self, url, output_path):
        ydl_opts = {
            'format': 'best[ext=mp4]',
            'outtmpl': output_path,
        }
        with yt_dlp.YoutubeDL(ydl_opts) as ydl:
            ydl.download([url])
```

---

### Bug #4: Отсутствует UI кнопка "Добавить риск вручную" ⚠️

**Файл:** `backend/templates/operators/workspace.html:122-133`

```html
<div class="triggers-list">
    {% if ai_triggers %}
        {% for trigger in ai_triggers %}
            {% include 'operators/partials/trigger_row.html' %}
        {% endfor %}
    {% else %}
        <div class="text-center text-muted py-4">
            <p>Нет обработанных триггеров</p>
        </div>
    {% endif %}
    <!-- ❌ Нет кнопки "Добавить риск вручную" -->
</div>
```

**Требование:** US-O9 - оператор должен иметь кнопку для ручного добавления рисков, пропущенных AI (False Negatives)

**Решение:**
Добавить после списка триггеров:
```html
<button class="btn btn-outline-primary w-100 mt-2" 
        onclick="showManualRiskForm()">
    <i class="bi bi-plus-circle"></i> Добавить риск вручную
</button>
```

---

## 📁 Отсутствующая документация

**Требование:** Согласно памяти и ТЗ (Модуль 1), должны существовать:
- `docs/API.md` - полная документация REST API
- `docs/ARCHITECTURE.md` - архитектурный обзор системы
- `docs/DEVELOPMENT.md` - руководство разработчика

**Текущее состояние:**
- ❌ Каталог `docs/` не существует
- ✅ `README.md` содержит краткую документацию (1147 строк)
- ⚠️ Swagger UI доступен на `/api/docs/` (автогенерация через drf-spectacular)

**Рекомендация:** 
Создать `docs/` с документацией согласно структуре из памяти (уже описано: 1141 строка в DEVELOPMENT.md, итд.)

---

## 📊 Итоговая оценка по модулям

| Модуль | Оценка | Статус |
|--------|--------|--------|
| 1. Архитектура | 9.5/10 | ✅ Отлично |
| 2. Роли | 10/10 | ✅ Идеально |
| 3. User Stories | 6.5/10 | ⚠️ Требует доработки |
| 4. AI/ML Подсистема | 7.5/10 | ⚠️ Словари отсутствуют |
| 5. UI/UX | 6/10 | ❌ Неполная карта разметки |
| 6. Нефункциональные | 8.5/10 | ✅ Хорошо |

**Общая оценка:** **7.5/10** (75% готовности MVP)

---

## 🔧 Roadmap исправлений

### Priority 1: Критичные баги (блокируют продакшен)

1. **Исправить RiskDefinition.code default**
   - Файл: `ai_pipeline/models.py:103`
   - Время: 5 минут
   - Риск: Высокий (IntegrityError при создании)

2. **Добавить NLP словари**
   - Создать: `ai_pipeline/dictionaries/*.txt`
   - Время: 2-4 часа (поиск датасетов + тестирование)
   - Риск: Высокий (система не работает корректно)

3. **Расширить FinalLabel enum**
   - Файл: `operators/models.py:62-68`
   - Добавить 10 недостающих категорий
   - Обновить `LabelingService.LABEL_MAPPING`
   - Создать миграцию Django
   - Время: 1-2 часа
   - Риск: **Критичный** (философия "Data-as-an-Asset" нарушена)

### Priority 2: Недостающие функции

4. **Реализовать загрузку видео по URL (US-C4)**
   - Создать: `VideoDownloader` сервис + Celery task
   - Время: 3-4 часа
   - Риск: Средний

5. **Добавить кнопку "Ручное добавление риска" (US-O9)**
   - Обновить: `operators/workspace.html` + JS обработчики
   - Время: 2-3 часа
   - Риск: Низкий

### Priority 3: Документация

6. **Создать docs/ каталог**
   - Файлы: `API.md`, `ARCHITECTURE.md`, `DEVELOPMENT.md`
   - Время: 4-6 часов
   - Риск: Низкий (не влияет на функциональность)

7. **Добавить admin статистику (US-A5)**
   - Custom Django Admin view с метриками
   - Время: 2-3 часа
   - Риск: Низкий

---

## ✅ Выводы

**Проект находится в хорошем состоянии** с точки зрения архитектуры и технического качества кода, но имеет **критические пробелы** в реализации бизнес-логики "Модели Кентавр".

**Главная проблема:** Неполная карта разметки (Модуль 5.2) нарушает философию "Data-as-an-Asset". Без 16+ категорий лейблов система не может собирать качественный датасет для обучения "умных" AI-моделей в v2.0.

**Рекомендуется:**
1. Немедленно исправить баг #1 (RiskDefinition.code)
2. В течение недели: добавить словари (баг #2) и расширить FinalLabel (баг #3)
3. В течение 2 недель: реализовать US-C4 (URL загрузка) и US-O9 (ручное добавление)
4. До релиза: создать полную документацию

**Оценка команды:** Разработка ведется профессионально, с хорошим пониманием современных практик (DRF, HTMX, Docker, Celery). Пробелы связаны скорее с приоритизацией задач, чем с техническими навыками.
