# 📝 Изменения - Конфигурация для деплоя через переменные окружения

## Дата: 25 ноября 2024

## Что было сделано

### 1. Создана полная конфигурация через Environment Variables

#### Новые файлы:
- **`.env.example`** - Полный шаблон конфигурации с комментариями на русском языке
  - Все обязательные переменные (Django, Database, Redis, Replicate, Backblaze B2)
  - Рекомендуемые переменные (Cloudflare, Email)
  - Опциональные настройки (Pipeline параметры, словари NLP)
  - Примеры значений и инструкции по получению ключей

- **`setup_env.sh`** - Скрипт быстрой настройки окружения
  - Автоматическое копирование `.env.example` → `.env`
  - Интерактивные подсказки по следующим шагам
  - Исполняемый (`chmod +x`)

- **`backend/compliance_app/config_validator.py`** - Валидатор конфигурации
  - Проверка обязательных переменных
  - Проверка рекомендуемых переменных
  - Валидация форматов (DATABASE_URL, REDIS_URL, REPLICATE_API_TOKEN)
  - Проверка безопасности (SECRET_KEY не содержит дефолтных значений)
  - Автоматический запуск при старте приложения (production)
  - Можно запускать вручную: `python backend/compliance_app/config_validator.py`

### 2. Обновлена конфигурация Django

#### `backend/compliance_app/settings.py`:
Добавлены переменные окружения:

**AI Модели (Replicate):**
- `WHISPER_MODEL_ID` - OpenAI Whisper для ASR
- `YOLO_MODEL_ID` - YOLOv8 для детекции объектов
- `NSFW_MODEL_ID` - Детектор 18+ контента
- `VIOLENCE_MODEL_ID` - Детектор жести/насилия
- `OCR_MODEL_ID` - EasyOCR для распознавания текста

**Email настройки:**
- `EMAIL_BACKEND` - SMTP/Console backend
- `EMAIL_HOST`, `EMAIL_PORT` - SMTP сервер
- `EMAIL_USE_TLS` - Использование TLS
- `EMAIL_HOST_USER`, `EMAIL_HOST_PASSWORD` - SMTP credentials
- `DEFAULT_FROM_EMAIL` - Email отправителя
- `ADMIN_EMAIL` - Email администратора

**Cloudflare дополнительно:**
- `CLOUDFLARE_API_TOKEN` - API токен
- `CLOUDFLARE_ZONE_ID` - Zone ID домена

**AI Pipeline:**
- `MAX_VIDEO_FILE_SIZE` - Макс. размер видео (default: 2GB)
- `MAX_VIDEO_DURATION` - Макс. длительность (default: 2 часа)
- `FRAME_EXTRACTION_FPS` - FPS для извлечения кадров (default: 1)
- `REPLICATE_TIMEOUT` - Timeout API запросов

**Словари NLP (опционально):**
- `PROFANITY_DICT_PATH` - Путь к словарю мата
- `BRAND_DICT_PATH` - Путь к словарю брендов
- `STOPWORDS_DICT_PATH` - Путь к словарю запрещенных слов

### 3. Интеграция валидации при запуске

#### Обновлены:
- **`backend/compliance_app/wsgi.py`** - Валидация перед запуском WSGI
- **`backend/compliance_app/asgi.py`** - Валидация перед запуском ASGI

Теперь при запуске в production режиме (DEBUG=False) автоматически проверяется наличие всех необходимых переменных. Если обязательные переменные не заданы - приложение не запустится с понятным сообщением об ошибке.

### 4. Улучшения в AI сервисах

#### `backend/ai_pipeline/services/ai_services.py`:
- **NLPDictionaryService**: Добавлена поддержка загрузки словарей из файлов
  - Метод `_load_dictionary()` загружает слова из файла (если путь задан)
  - Fallback на дефолтные словари если файл не найден
  - Поддержка кириллицы (encoding='utf-8')
  - Добавлена проверка на `stopwords` (запрещенные слова 436-ФЗ, 20.3 КоАП)

#### `backend/ai_pipeline/services/ffmpeg_service.py`:
- **FrameProcessor**: Использование `FRAME_EXTRACTION_FPS` из настроек
  - Параметр FPS теперь берется из settings
  - Возможность переопределить через аргумент метода

### 5. Улучшен .gitignore

Добавлены дополнительные исключения:
- `.env*` файлы (все варианты)
- Временные файлы и директории
- Логи Django и Celery
- Coverage отчеты
- PyCharm/VS Code конфиги
- Backup файлы

### 6. Документация

#### Создана подробная документация:

**`README.md`** - Основное описание проекта:
- Описание системы
- Быстрый старт
- Архитектура и технологический стек
- Структура проекта
- Получение API ключей
- Команды разработки
- Мониторинг
- Оценка затрат

**`CONFIGURATION.md`** - Руководство по конфигурации:
- Список всех переменных (обязательные/рекомендуемые/опциональные)
- Подробное описание каждой переменной
- Инструкции по получению API ключей
- Примеры конфигураций (dev/production)
- Troubleshooting распространенных проблем
- Валидация конфигурации
- Генерация SECRET_KEY

**`DEPLOYMENT.md`** - Руководство по деплою:
- Пошаговая инструкция деплоя на DigitalOcean App Platform
- Настройка компонентов (Web Service, Celery Worker, Databases)
- Конфигурация Environment Variables в DigitalOcean
- Настройка внешних сервисов (Backblaze B2 CORS, Cloudflare Page Rules, Replicate billing)
- Проверка работоспособности
- Troubleshooting проблем деплоя
- Мониторинг и метрики
- Масштабирование
- Security checklist

## Что изменилось для разработчика

### До:
```python
# Hardcoded значения в settings.py
REPLICATE_API_TOKEN = 'your_api_token'
# Нет проверки обязательных переменных
```

### После:
```python
# Все через environment variables
REPLICATE_API_TOKEN = env('REPLICATE_API_TOKEN', default='')
if not REPLICATE_API_TOKEN and not DEBUG:
    raise ValueError("REPLICATE_API_TOKEN must be set in production")
```

## Процесс деплоя

### Раньше:
1. Править settings.py с реальными credentials
2. Искать места, где захардкожены ключи
3. Не было проверки корректности конфигурации

### Теперь:
1. `cp .env.example .env` (или `./setup_env.sh`)
2. Заполнить .env файл (все переменные с комментариями)
3. `python backend/compliance_app/config_validator.py` - проверить
4. Деплой (credentials автоматически подхватываются из .env или App Platform Environment Variables)
5. Автоматическая валидация при запуске

## Безопасность

✅ `.env` файл в `.gitignore` - не попадет в репозиторий
✅ Автоматическая проверка на дефолтные/небезопасные значения SECRET_KEY
✅ Валидация форматов URL (postgres://, redis://, r8_)
✅ Требование SSL для production (DATABASE_URL sslmode=require)

## Обратная совместимость

✅ Все изменения обратно совместимы
✅ Дефолтные значения для необязательных параметров
✅ Существующие деплои продолжат работать (но рекомендуется мигрировать на .env)

## Следующие шаги

1. Заполните `.env` файл реальными credentials
2. Проверьте конфигурацию: `python backend/compliance_app/config_validator.py`
3. Протестируйте локально: `python backend/manage.py runserver`
4. Следуйте `DEPLOYMENT.md` для production деплоя

## Файлы для review

- `.env.example` - проверьте комментарии и примеры
- `backend/compliance_app/settings.py` - новые переменные окружения
- `backend/compliance_app/config_validator.py` - логика валидации
- `CONFIGURATION.md` - документация по конфигурации
- `DEPLOYMENT.md` - документация по деплою
- `README.md` - обновленное описание проекта

---

**Готово к деплою! 🚀**
